// 3 Sum - Find all unique triplets in the array which gives the sum of zero.

// For example, given array S = [-1, 0, 1, 2, -1, -4],
// Sum = 0; and no duplicates

Algo:
    - We will need to loop over the Array
    - if we keep one value constant we can manage to detect the duplicate
    - if we find we have the node visited we will skip
